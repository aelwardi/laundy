{% extends 'base.html.twig' %}

{% block title %}Tarification - {{ parent() }}{% endblock %}

{% block content %}

  <section class="bg-blue-500 pb-8 text-center pt-20">
    <h1 class="text-4xl font-bold text-white mb-3">Tarification simplifiée</h1>
    <p class="text-lg text-white opacity-80">Pas de frais cachés.</p>
  </section>


  <div class="bg-torea-900 text-white flex flex-col md:flex-row justify-center items-center gap-6 py-4 text-sm font-medium h-25">
    <div class="flex items-center gap-2">
      <span class="inline-flex items-center justify-center w-8 h-8 rounded-full mr-2">
        <img src="svg3.svg" alt="icon" class="w-8 h-8" />
      </span>
      Livraison gratuite en 24h
    </div>
    <div class="flex items-center gap-2">
      <span class="inline-flex items-center justify-center w-8 h-8 rounded-full mr-2">
        <img src="svg3.svg" alt="icon" class="w-8 h-8" />
      </span>
      Montant minimum de commande de €25
    </div>
    <div class="flex items-center gap-2">
      <span class="inline-flex items-center justify-center w-8 h-8 rounded-full mr-2">
        <img src="svg11.svg" alt="icon" class="w-8 h-8" />
      </span>
      Frais de service à partir de €2,99
    </div>
  </div>


  <main class="max-w-5xl mx-auto flex flex-col gap-10 mt-10">
    <div class="flex flex-col md:flex-row justify-center gap-6">
      {% for service in services %}
      <a href="{{ path('app_tarif_details', {'id': service.id}) }}" 
         class="flex-1 bg-white border border-gray-400 rounded-xl shadow-sm flex flex-col items-center p-6 min-w-[260px] max-w-xs mx-auto hover:bg-blue-50 hover:shadow-md transform hover:scale-105 transition-all duration-300 group">
        <div class="w-20 h-20 rounded-full flex items-center justify-center bg-blue-100 mb-4 group-hover:bg-blue-200 transition-colors duration-300">
          <img src="{{ service.image }}" alt="{{ service.name }}" class="w-12 h-12 object-contain"/>
        </div>
        <div class="font-bold text-lg mb-1 text-gray-800 group-hover:text-blue-600 transition-colors duration-300">{{ service.name }}</div>
        <div class="text-gray-600 text-sm mb-4 text-center leading-relaxed">{{ service.description }}</div>
        <div class="w-full border-t border-gray-300 pt-4 mt-auto">
          <div class="text-xs text-gray-500 font-medium uppercase tracking-wide mb-2">Prix par poids</div>
          <div class="text-sm text-gray-700">
            à partir de 
            <span class="text-blue-600 font-bold text-lg">
              {% if service.name == "Laver" %}
                €24.90</span><span class="text-gray-500 text-sm">/6kg
              {% elseif service.name == "Pressing" %}
                €3</span><span class="text-gray-500 text-sm">/pièce
              {% elseif service.name == "Couettes & Ameublements" %}
                €14</span><span class="text-gray-500 text-sm">/pièce
              {% endif %}
            </span>
          </div>
        </div>
      </a>
      {% endfor %}
    </div>
    <div class="bg-water-200 rounded-lg p-5 flex flex-col md:flex-row items-center justify-between  ">
      <div>
        <span class="font-semibold text-torea-900">Vous avez une question sur nos services et nos prix ?</span><br />
        <span class="text-torea-900 text-sm">Demandez à notre sympathique chatbot Washy !</span>
      </div>
      <a href="{{ path('app_ticket') }}" class="bg-white text-blue-500 px-4 py-2 rounded font-semibold shadow hover:bg-blue-100 mt-3 md:mt-0">Démarrer le chat</a>
    </div>
  </main>
{% endblock %}