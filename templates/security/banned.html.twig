<!doctype html>
<html lang="fr" class="h-full">

<head>
  <meta charset="utf-8">
  <title>{% block title %}Accès Interdit - Laundry{% endblock %}</title>
  <base href="/">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" type="image/x-icon" href="favicon.ico">
  {% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('app.css') }}">
  {% endblock %}
</head>

<body class="h-full bg-gradient-to-br from-slate-50 via-gray-50 to-zinc-100 font-sans antialiased">
  <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-from)_0%,_transparent_50%)] from-red-100/20"></div>
  
  <div class="relative min-h-screen flex items-center justify-center px-4 py-12 sm:px-6 lg:px-8">
    <div class="w-full max-w-lg">
      <div class="bg-white/90 backdrop-blur-sm rounded-3xl shadow-2xl border border-white/20 overflow-hidden">
        
        <div class="px-8 pt-12 pb-8 text-center bg-gradient-to-b from-red-50/50 to-transparent">
          <div class="mx-auto w-20 h-20 mb-6 relative">
            <div class="absolute inset-0 bg-red-100 rounded-full animate-pulse"></div>
            <div class="relative w-full h-full bg-gradient-to-br from-red-500 to-red-600 rounded-full flex items-center justify-center shadow-lg">
              <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728L5.636 5.636m12.728 12.728L18.364 5.636M5.636 18.364l12.728-12.728"></path>
              </svg>
            </div>
          </div>
          
          <h1 class="text-4xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent mb-2">
            Accès Interdit
          </h1>
          <p class="text-gray-600 text-lg font-medium">Erreur 403</p>
        </div>

        <div class="px-8 pb-8">
          <div class="relative mb-8 p-6 bg-gradient-to-r from-red-50 to-rose-50 border border-red-100 rounded-2xl">
            <div class="flex items-start space-x-4">
              <div class="flex-shrink-0">
                <div class="w-10 h-10 bg-red-100 rounded-full flex items-center justify-center">
                  <svg class="w-5 h-5 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z"></path>
                  </svg>
                </div>
              </div>
              <div class="flex-1">
                <h3 class="text-lg font-semibold text-red-900 mb-2">
                  Compte Suspendu
                </h3>
                <p class="text-red-700 text-sm leading-relaxed mb-4">
                  Votre compte a été temporairement suspendu. Vous n'avez plus accès aux fonctionnalités de l'application.
                </p>
                
                {% if user %}
                <div class="bg-white/70 rounded-xl p-4 border border-red-200/50">
                  <div class="space-y-2 text-sm">
                    <div class="flex justify-between">
                      <span class="font-medium text-gray-700">Compte :</span>
                      <span class="text-gray-900 font-mono">{{ user.email }}</span>
                    </div>
                    <div class="flex justify-between">
                      <span class="font-medium text-gray-700">Statut :</span>
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                        Suspendu
                      </span>
                    </div>
                    <div class="flex justify-between">
                      <span class="font-medium text-gray-700">Rôles :</span>
                      <span class="text-gray-900 font-mono text-xs">{{ user.roles|join(', ') }}</span>
                    </div>
                  </div>
                </div>
                {% endif %}
              </div>
            </div>
          </div>

          <div class="text-center mb-8">
            <p class="text-gray-600 leading-relaxed">
              Si vous pensez qu'il s'agit d'une erreur, n'hésitez pas à contacter notre équipe de support.
            </p>
          </div>

          <div class="flex flex-col sm:flex-row gap-3 mb-6">
            <a href="{{ path('app_logout') }}" 
               class="flex-1 inline-flex items-center justify-center px-6 py-3 border border-transparent text-base font-medium rounded-xl text-white bg-gradient-to-r from-red-600 to-red-700 hover:from-red-700 hover:to-red-800 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
              </svg>
              Se Déconnecter
            </a>
            
            <a href="mailto:support@laundy.com" 
               class="flex-1 inline-flex items-center justify-center px-6 py-3 border border-gray-200 text-base font-medium rounded-xl text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-all duration-200 shadow-md hover:shadow-lg transform hover:-translate-y-0.5">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
              </svg>
              Contacter le Support
            </a>
          </div>

          <div class="pt-6 border-t border-gray-100">
            <div class="text-center">
              <p class="text-xs text-gray-500 leading-relaxed">
                <span class="font-semibold">Code d'erreur :</span> 403 - Accès Refusé<br>
                <span class="text-gray-400">Si le problème persiste, contactez notre équipe technique.</span>
              </p>
              <div class="mt-3 flex items-center justify-center space-x-2 text-xs text-gray-400">
                <span>Laundry</span>
                <span>•</span>
                <span>Système de Sécurité</span>
                <span>•</span>
                <span>{{ "now"|date("Y") }}</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="mt-6 text-center">
        <p class="text-xs text-gray-400">
          Cette page est protégée par notre système de sécurité avancé
        </p>
      </div>
    </div>
  </div>
</body>

</html>