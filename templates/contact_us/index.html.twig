{% extends 'base.html.twig' %}

{% block title %}Contactez Nous - {{ parent() }}{% endblock %}

{% block content %}
<style>
  .contact-gradient {
    background: linear-gradient(135deg, #1e40af, #3b82f6);
  }
  
  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
    opacity: 0;
  }
  
  @keyframes fade-in-up {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }
</style>
  <section class="contact-gradient py-20 relative overflow-hidden">
    <div class="absolute inset-0 z-0">
      <svg viewBox="0 0 1440 464" fill="none" xmlns="http://www.w3.org/2000/svg" class="w-full h-full">
        <ellipse cx="720" cy="0" rx="800" ry="350" fill="rgba(255,255,255,0.1)" />
      </svg>
    </div>
    
    <div class="max-w-6xl mx-auto px-4 relative z-10">
      <div class="text-center text-white">
        <h1 class="text-4xl md:text-6xl font-bold mb-4 animate-fade-in-up">
          Contactez <span class="text-yellow-300">Nous</span>
        </h1>
        <p class="text-xl mb-8 animate-fade-in-up" style="animation-delay: 0.3s;">
          Notre équipe est là pour répondre à toutes vos questions
        </p>
      </div>
    </div>
  </section>

  <div class="max-w-6xl mx-auto px-4 -mt-10 relative z-20">
    <section class="text-center mb-16">
      <div class="bg-white rounded-3xl shadow-xl p-8 animate-fade-in-up" style="animation-delay: 0.6s;">
        <h2 class="text-2xl md:text-3xl font-bold text-gray-800 mb-4">
          Un service client <span class="text-blue-600">d'exception</span>
        </h2>
        <p class="text-gray-600 text-lg">
          Quelle que soit votre demande, nos conseillers français sont à votre écoute pour vous accompagner.
        </p>
      </div>
    </section>

    <section class="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
      <div class="flex justify-center items-center animate-fade-in-up" style="animation-delay: 0.8s;">
        <div class="relative">
          <img src="img4.png" alt="Service client" class="rounded-2xl shadow-lg max-w-full h-auto">
          <div class="absolute inset-0 bg-gradient-to-t from-blue-600/20 to-transparent rounded-2xl"></div>
        </div>
      </div>
      
      <div class="space-y-8 animate-fade-in-up" style="animation-delay: 1s;">
        <div>
          <h3 class="text-3xl font-bold text-gray-800 mb-4">Contacter Laundry</h3>
          <p class="text-gray-600 text-lg mb-6">
            Nos conseillers, tous localisés en France, sont à votre écoute du lundi au vendredi de 8h à 20h et le samedi de 9h à 17h.
          </p>
        </div>
        
        <div class="space-y-4">
          <a href="tel:0100000000" 
             class="flex items-center bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white px-6 py-4 rounded-2xl shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300 group">
            <div class="w-12 h-12 bg-white/20 rounded-xl flex items-center justify-center mr-4 group-hover:scale-110 transition-transform duration-300">
              <img src="svg9.svg" alt="Téléphone" class="w-6 h-6 filter brightness-0 invert" />
            </div>
            <div>
              <div class="font-semibold text-lg">01 00 00 00 00</div>
              <div class="text-sm opacity-90">Appel non surtaxé</div>
            </div>
          </a>
          
          <a href="#" 
             class="flex items-center bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 text-white px-6 py-4 rounded-2xl shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300 group">
            <div class="w-12 h-12 bg-white/20 rounded-xl flex items-center justify-center mr-4 group-hover:scale-110 transition-transform duration-300">
              <img src="svg10.svg" alt="Localisation" class="w-6 h-6 filter brightness-0 invert" />
            </div>
            <div>
              <div class="font-semibold text-lg">Nous localiser</div>
              <div class="text-sm opacity-90">Voir sur Google Maps</div>
            </div>
          </a>
        </div>
      </div>
    </section>

    <section class="bg-gradient-to-br from-blue-50 to-indigo-50 rounded-3xl p-8 md:p-12 shadow-xl relative overflow-hidden animate-fade-in-up" style="animation-delay: 1.2s;">
      <div class="absolute inset-0 opacity-5">
        <svg class="w-full h-full" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
          <defs>
            <pattern id="contact-grid" width="10" height="10" patternUnits="userSpaceOnUse">
              <path d="M 10 0 L 0 0 0 10" fill="none" stroke="currentColor" stroke-width="1"/>
            </pattern>
          </defs>
          <rect width="100" height="100" fill="url(#contact-grid)" />
        </svg>
      </div>
      
      <div class="relative z-10">
        <h3 class="text-3xl font-bold text-gray-800 mb-2 text-center">Écrivez-nous</h3>
        <p class="text-gray-600 text-center mb-8">Laissez-nous un message et nous vous répondrons rapidement</p>
        
        <form class="space-y-6 max-w-2xl mx-auto">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">Prénom</label>
              <input type="text" 
                     class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 outline-none transition-all duration-300 bg-white/80 backdrop-blur-sm"
                     placeholder="Votre prénom">
            </div>
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">Nom</label>
              <input type="text" 
                     class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 outline-none transition-all duration-300 bg-white/80 backdrop-blur-sm"
                     placeholder="Votre nom">
            </div>
          </div>
          
          <div>
            <label class="block text-sm font-semibold text-gray-700 mb-2">Email</label>
            <input type="email" 
                   class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 outline-none transition-all duration-300 bg-white/80 backdrop-blur-sm"
                   placeholder="votre@email.com">
          </div>
          
          <div>
            <label class="block text-sm font-semibold text-gray-700 mb-2">Sujet</label>
            <input type="text" 
                   class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 outline-none transition-all duration-300 bg-white/80 backdrop-blur-sm"
                   placeholder="Objet de votre message">
          </div>
          
          <div>
            <label class="block text-sm font-semibold text-gray-700 mb-2">Message</label>
            <textarea rows="6" 
                      class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 outline-none transition-all duration-300 bg-white/80 backdrop-blur-sm resize-none"
                      placeholder="Écrivez votre message..."></textarea>
          </div>
          
          <div class="text-center">
            <button type="submit" 
                    class="inline-flex items-center bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white px-8 py-4 rounded-xl font-semibold shadow-lg hover:shadow-xl transform hover:scale-105 hover:-translate-y-1 transition-all duration-300">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
              </svg>
              Envoyer le message
            </button>
          </div>
        </form>
      </div>
    </section>
  </div>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver(function(entries) {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.opacity = '1';
        entry.target.style.transform = 'translateY(0)';
      }
    });
  }, observerOptions);

  document.querySelectorAll('.animate-fade-in-up').forEach(el => {
    observer.observe(el);
  });

  document.querySelectorAll('input, textarea').forEach(input => {
    input.addEventListener('focus', function() {
      this.parentElement.classList.add('focused');
    });
    
    input.addEventListener('blur', function() {
      if (!this.value) {
        this.parentElement.classList.remove('focused');
      }
    });
  });
});
</script>
{% endblock %}